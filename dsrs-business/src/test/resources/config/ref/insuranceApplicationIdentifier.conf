# the validation rules for the insuranceApplicationId
# this file will be included anywhere that takes in a insuranceApplicationId and needs to be validated
# (notice)
insuranceApplicationIdentifier:{
  dataType: Number
  validationRules: ["MinLength==1", "MaxLength==30"]
}