# the validation rules for the dsrsId
# this file will be included anywhere that takes in a dsrsId and needs to be validated
# (metadata_update and metadata_retrieval)
dsrsId {
  dataType: String
  validationRules: ["MinLength==1", "MaxLength==30"]
}