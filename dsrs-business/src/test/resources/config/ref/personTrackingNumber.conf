# the validation rules for the personTrackingNumber
# this file will be included anywhere that takes in a PTN and needs to be validated
# (notice, mailed_supporting_doc, uploaded_supporting_doc)
personTrackingNumber: {
  dataType: String
  validationRules: ["MinLength==1", "MaxLength==30", "NumericCharacters"]
}