# the validation rules for the exemptionApplicationId
# this file will be included anywhere that takes in an eai and needs to be validated
# (notice)
exemptionApplicationIdentifier: {
  dataType: Number
  validationRules: ["MinLength==1", "MaxLength==30"]
}