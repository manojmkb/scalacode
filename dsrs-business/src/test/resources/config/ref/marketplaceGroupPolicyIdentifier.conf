# the validation rules for the marketplaceGroupPolicyId
# this file will be included anywhere that takes in a marketplaceGroupPolicyId and needs to be validated
# (notice)
marketplaceGroupPolicyIdentifier: {
  dataType: Number
  validationRules: ["MinLength==1", "MaxLength==30"]
}