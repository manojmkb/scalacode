//Cache-sizing for Ehcache
cache: {
  name: "dsrsCache"

  // Cache name for reference data
  // The maximum number of cache entries or bytes a cache can use in local heap memory
  refDataCache: {
    maxEntriesLocalHeap: 50
  }
}